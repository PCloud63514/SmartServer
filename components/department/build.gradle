dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation(project(":components:library-department"))
    implementation(project(":components:test-support"))
}

test {
    useJUnitPlatform()
}