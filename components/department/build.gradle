dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation(project(":components:domain-department"))
    implementation(project(":components:test-support"))
}

test {
    useJUnitPlatform()
}